// Test file to verify parser error fixes work correctly
def factorial(n: int) -> int {
    if n <= 1 {
        return 1;
    } else {
        return n * factorial(n - 1);
    }
}

def main() -> int {
    let result = factorial(5);
    return result;
}